import { Layout } from "antd";

const {Header} = Layout;
const AppHeader = () => {
    return(
        <Header style={{ background: "#001529", color: "#fff" }}>
            My dashbord
        </Header>
    );
}

export default AppHeader;